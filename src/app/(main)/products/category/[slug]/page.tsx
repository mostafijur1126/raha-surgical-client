// type PageProps = {
//   params: Promise<{
//     slug: string;
//   }>;
// };

// const page = async ({ params }: PageProps) => {
//   const { slug } = await params;
//   console.log(slug);
//   return <div>Dynamic Route। Category: {slug}</div>;
// };

// export default page;
